interface:
  display_name: "Opciones Migration Control Plane"
  short_description: "Apply the opciones-byma migration manifest, gates, parity, and decision workflow"
  default_prompt: "Use $opciones-migration-control-plane to implement this migration issue and prepare its cross-agent handoff."
